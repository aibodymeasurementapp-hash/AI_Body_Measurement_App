name: ai_body_measure_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  go_router: ^13.2.0
  flutter_riverpod: ^2.5.1
  image_picker: ^1.0.7
  camera: ^0.10.5+9

  # AI model + image processing
  tflite_flutter: ^0.12.1
  image: ^4.1.7

  # ── NEW: Gyroscope / accelerometer sensor access ──────────────────
  sensors_plus: ^4.0.2

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/models/movenet_thunder.tflite