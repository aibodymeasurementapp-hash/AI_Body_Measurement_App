name: ai_body_measure_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  go_router: ^13.2.0
  flutter_riverpod: ^2.5.1
  image_picker: ^1.0.7
  camera: ^0.10.5+9

  # AI model + image processing
  # Ftflite_flutter: ^0.12.1
  google_mlkit_pose_detection: ^0.12.0

  image: ^4.1.7

  # ── NEW: Gyroscope / accelerometer sensor access ──────────────────
  sensors_plus: ^4.0.2
  cupertino_icons: ^1.0.8
  # payment mathod
  #http: ^1.2.0
  #webview_flutter: ^4.4.0
  firebase_core: ^4.7.0
  firebase_auth: ^6.0.0
  cloud_firestore: ^6.3.0
  #provider: ^6.1.2
  #uuid: ^4.3.3
  purchases_flutter: ^9.8.0       # ← must be 9.8.0+ for test_ key to work
  purchases_ui_flutter: ^9.8.0    # ← must match purchases_flutter version



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
      - assets/images/men/pant_shirt/
      - assets/images/men/shalwar_qameez/